import { Text, View } from "react-native";

export default function streaksScreen() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text style={{ fontSize: 20 }}>Hi there, this is the log-in page</Text>
    </View>
  );
}
